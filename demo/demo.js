$(document).ready(function() {
	
	_tipzy.init();	
	
	_tipzy.addTip($("#demo_add_later"), 'I manually added this on docready');
	_tipzy.addTip($("#demo_add_later2"), 'I manually added this on docready- also as well too');

});
